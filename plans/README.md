# Implementation Plans

Generated by the improve skill on 2026-07-01 (006 added 2026-07-02). Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title                                                                  | Priority | Effort | Depends on    | Status |
| ---- | ---------------------------------------------------------------------- | -------- | ------ | ------------- | ------ |
| 001  | Collapse the public API to one simulation function                     | P1       | S      | -             | DONE   |
| 002  | Remove redundant `requiredAllowance` from deltas                       | P1       | S      | 001           | DONE   |
| 003  | Remove public diagnostics from simulation results                      | P2       | S      | 001, 002      | DONE   |
| 004  | Cut allowance attribution re-simulation                                | P2       | M      | 001, 002, 003 | DONE   |
| 005  | Fix published package files                                            | P2       | S      | -             | DONE   |
| 006  | Split slot discovery out of simulate(); caller-supplied slot overrides | P1       | M      | -             | DONE   |
| 007  | Optional discoverRequirements() — measure required balances/approvals  | P2       | L      | 006           | DONE   |
| 008  | CI verification baseline (Actions, pinned foundry nightly, anvil fix)  | P1       | M      | -             | DONE   |
| 009  | Fix permit/approve-overwrite corruption of allowance measurement       | P1       | M      | -             | DONE   |
| 010  | Clear high-severity `ws` advisory (lockfile refresh)                   | P2       | S      | -             | DONE   |
| 011  | Error-path hardening (wrap decode) + error test coverage               | P2       | M      | -             | DONE   |
| 012  | Internal duplication cleanup (call params, slots dup, block options)   | P3       | M      | 009, 011      | DONE   |
| 013  | Parallelize independent RPC calls                                      | P3       | M      | 009, 012      | DONE   |
| 014  | CLAUDE.md architecture brief + README link + motivation.md note        | P3       | S      | -             | DONE   |
| 015  | NFT receiver hooks in TxSimulator + contract-wallet semantics docs     | P2       | S-M    | -             | DONE   |
| 016  | viem-convention renames (`calldata`→`data`, public `owner`→`from`)     | P1       | S      | -             | DONE   |
| 017  | Move base-slot inference into public discoverAllowanceSlots            | P1       | M      | 016           | DONE   |
| 018  | Discriminated result unions (SimulationResult, DiscoveredRequirements) | P2       | M      | 016           | DONE   |
| 019  | Report unresolved tokens/pairs from discovery                          | P2       | M      | 016, 017, 018 | DONE   |
| 020  | Internal shape polish (RpcCallArgs, override docs, decode-cast)        | P3       | S-M    | 016-019       | DONE   |
| 021  | TxSimulator instance API (interface + companion create())              | P2       | M      | 016-019       | DONE   |
| 022  | API documentation pass (JSDoc, exported defaults, error guidance)      | P3       | M      | 016-021       | DONE   |
| 023  | Right-side-up arg types (client-less public Args, file consolidation)  | P1       | M      | -             | DONE   |
| 024  | Decode custom-error reverts (optional errorAbi, selector fallback)     | P2       | M      | -             | DONE   |
| 025  | viem-style restructure (public-only top level, internal/ → 6 domains)  | P2       | M      | 024           | DONE   |
| 026  | TokenSlotOverride as pipeline currency (required amount, deal docs)    | P2       | S-M    | -             | DONE   |
| 027  | Rename preparation and requirement APIs around caller intent            | P1       | M      | 026           | DONE   |
| 028  | npm consumption readiness (peer-dep viem, manifest, tarball CI checks) | P1       | M      | -             | DONE   |
| 029  | Release automation (changesets, OIDC trusted publishing)               | P2       | S-M    | 028, 030      | DONE   |
| 030  | Supply-chain hardening (pinned tooling, SHA actions, cooldown)         | P1       | S-M    | 028           | DONE   |
| 031  | Explicit balance queries — discovery-free simulate, arbitrary accounts | P1       | L      | -             | DONE   |
| 032  | Helper namespaces (balanceQueries.*, tokenOverrides.*)                 | P2       | S-M    | 031           | DONE   |
| 033  | Align CI node with release pin + hygiene (gitignore, permit doc)       | P2       | S      | -             | DONE   |
| 034  | Per-call balance attribution (BalanceDelta.byCall via checkpoints)     | P2       | M      | -             | DONE   |
| 035  | README orientation pass (API-at-a-glance, mental model, byCall fix)    | P3       | S      | -             | DONE   |
| 036  | Native balance overrides (forge ETH; estimateRequirements fallback)    | P1       | S-M    | -             | DONE   |
| 037  | Environment-gated publishing (approval on release, CODEOWNERS)         | P2       | S-M    | -             | DONE   |
| 038  | Strip dead per-token delta path from ghost contract (bytecode regen)   | P2       | S      | -             | DONE   |
| 039  | Classify provider reverts/insufficient-funds on structured signals     | P1       | M      | -             | DONE   |
| 040  | Guard hand-written simulator ABI against forge-artifact drift          | P1       | S      | -             | DONE   |
| 041  | Sync AGENTS.md/CLAUDE.md/README with the shipped architecture          | P2       | S      | -             | DONE   |
| 042  | Close named test-coverage gaps (debug sink, blocks, estimator reverts) | P2       | S-M    | 039           | DONE   |
| 043  | Hygiene sweep (parallel prepares, word parse, aliases, floors, cache)  | P3       | S      | 039           | DONE   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

Plan-authoring note: a plan's in-scope file list must include `plans/README.md` (the status-row update every plan requires) and any files its steps regenerate (e.g. `pnpm-lock.yaml`), so the scope boundary never contradicts the plan's own done criteria. (Plans 041–043 shipped with this contradiction; executors resolved it correctly, and those files stay unedited as the historical record.)

## Dependency notes

- 002 follows 001 to avoid updating tests and README twice for API names.
- 003 follows 001 and 002 because it touches the same public result types and tests.
- 004 follows 001-003 because it edits the same simulation result and attribution code after the public API is simplified.
- 005 is independent and can run anytime.
- 006 assumes 001-004 have landed (it was planned against the working tree containing their executed changes) and replaces the retry/attribution pipeline that 004 simplified.
- 007 hard-depends on 006 (builds on its slot-override API and single-pass simulate); its drift check refuses to run until 006 is DONE. It extends TxSimulator.sol, so it is the first plan requiring bytecode regeneration.
- 008 first among 008-014: it is the verification baseline the others' done criteria lean on (`pnpm verify`), though none hard-require it.
- 009 before 012 and 013: all three touch `src/requirements.ts`; 009 is the bug fix and must not rebase onto refactored code.
- 011 before 012: both touch `src/internal/simulator.ts` (011 wraps the decode; 012 moves `buildCallParameters`).
- 012 before 013: parallelize the consolidated implementations, not the duplicates 012 deletes.
- 010 and 014 are independent and can run anytime.
- 015 is independent of 009-013 but edits README like 014 — whichever lands second rebases a small docs edit. It changes TxSimulator.sol, so regenerated `src/generated/` + `dist/` must ship with it (CI freshness gate from 008).
- 016 first in the 016-022 wave: every later plan is written against its renamed fields.
- 017 creates `src/internal/allowanceDiscovery.ts` whose per-pair return shape 019 builds on; 018 reshapes the result types 019 extends — hence 019 after both.
- 020 is mechanical cleanup over files 016-019 touch; run it after them to avoid rebasing.
- 021 wraps the settled surface (016-019) and makes the instance API the ONLY public one (maintainer decision 2026-07-02: pre-prod, no dual surface): `index.ts` drops the four function exports, existing tests migrate to instance call style. 022 documents everything (JSDoc lands on the interface methods, not the internal module functions) and must run last.
- 023 inverts the type layering 021 left behind (maintainer finding 2026-07-03): public arg types are client-less, implementations take `& ClientArgs`, no `Omit`/`Parameters` gymnastics on the interface. It deletes `src/simulate.ts` and `src/slots.ts` (slot bodies go to `internal/slotDiscovery.ts` to avoid a requirements→txSimulator cycle; requirements.ts stays separate by size).
- 024 is independent; touches TS only (revert bytes already flow through the contract unchanged). `errorAbi` goes on `TxSimulatorConfig`/`SimulateArgs`/`DiscoverRequirementsArgs` only — never slot-discovery args.
- 025 hard-depends on 024 (moves/rewrites the files 024 edits: revert.ts, requirements.ts, the defaults() merge). Zero behavior change: suite must pass with zero test-file edits. Rejected within it (don't re-propose): moving `src/generated/` (CI + generator path refs), probes-into-slots merge (size), errors/constants into types.ts, splitting types/ into a directory.
- 026 (maintainer design, 2026-07-03): discovery outputs ARE `TokenSlotOverride`s by declared type; `amount` required and discovery-populated; `amount === maxUint256` throws (decrement-blindness + receive-overflow footguns); `unresolved` docs reframed in Foundry `deal` vocabulary. Rejected within it: renaming `unresolved` or `tokenSlotOverrides`, flattening allowance slots to bare overrides (loses the spender needed for caching), `prank`/`impersonate` vocabulary (wrong Foundry analog — sender spoofing never fails; `deal` is the failing operation).
- 027 depends on 026 because the new preparation names rely on discovery outputs already being declared ready-to-use `TokenSlotOverride`s.
- 028 is pure packaging (no src/test changes): viem moves to peerDependencies (`2.x`), LICENSE created, tarball trimmed (drops 3.3 MB of docs PNGs, adds `src` so sourcemaps resolve), `prepare`/`prepublishOnly` scripts (tsc-only — committed bytecode means no Foundry for consumers), ci.yml gate pathspec corrected, attw + packed-tarball smoke test added to CI. Rejected within it: CJS build (ESM-only is the decided posture, documented in README); committing `dist/` (maintainer decision 2026-07-03 — standard ignored-build-output posture; note that earlier index/plan notes claiming "dist is committed" were WRONG, an unvetted audit claim: `dist/` has always been gitignored, the ci.yml gate's `dist` pathspec was vacuous, and git-dependency installs were broken until 028's `prepare` script).
- 029 hard-depends on 028 (publishes the manifest it makes publishable) and runs AFTER 030 so the release workflow is born hardened. Release concerns live only in `release.yml` — `ci.yml` stays secret-free and fork-runnable (plan 008 rule). Amended 2026-07-03 after the supply-chain review: auth is npm Trusted Publishing (OIDC) — NO long-lived NPM_TOKEN in the primary flow; `baseBranch`/trigger corrected to `master` (the repo's actual default branch — an earlier draft wrongly said `main`). Operator TODOs: configure the npmjs.com trust relationship, check name availability.
- 030 (from the 2026-07-03 supply-chain review; execute before 029): attw moves from `pnpm dlx` to a lockfile-pinned devDependency; all action refs SHA-pinned with version comments (tj-actions-style tag-moving is the threat); `permissions: contents: read` on ci.yml; pnpm `minimumReleaseAge: 4320` cooldown + documented single-grant `onlyBuiltDependencies` posture; smoke install pinned + `--ignore-scripts`; Dependabot for pin freshness. Rejected within it: StepSecurity harden-runner and zizmor/Scorecard (operationally heavy / marginal for a two-workflow repo — revisit if workflows multiply).

- 031/032 implement the ACCEPTED parts of the 2026-07-03 external API feedback (v2, code-informed). 031 is the breaking core: `balanceQueries` required on simulate (arbitrary-account observation, before/after/delta mirrored 1:1, `unresolved` per plan-019 convention, `before` = post-override), discovery extracted to `balanceQueries.forUser` (simulate = 1 eth_call, zero access lists, works without `eth_createAccessList`), contract gains `BalanceProbe[]`, `AssetBalanceDelta` deleted. 032 regroups helpers into `balanceQueries.*`/`tokenOverrides.*`, deleting the flat plan-027 method names (no aliases — pre-prod, maintainer instruction). Rejected from that feedback (do not re-propose): `{kind}` Asset object union (026-era reasoning — members differ in no other field); required-empty `tokenSlotOverrides` (absence is unambiguous); `diagnostics` on public results (plan 003 removed public diagnostics deliberately; checkpoint/outflow exposure = the deferred per-call attribution finding; the feedback's own sketch was self-inconsistent, putting discovery data on a discovery-free simulate); a first-class `approvals` param (feedback itself converged on approve-calls-in-batch or allowance overrides — document both); `forErc20s`/`fromAccessList` mapper helpers (one-line `.map`s); required `value` on calls (ERC-5792 pass-through); `error` wrapper object and `Promise<TokenSlotOverride[]>` lossy estimate return (018/024 shapes and full requirements data stand); `{owner, approvals}` arg naming on forAllowances (settled `from`/`pairs` vocabulary).

- 033/034 (2026-07-04, post-release — `viem-tx-sim` 0.1.0/0.1.1 are LIVE on npm; branch protection + Dependabot active): 033 is config/docs hygiene (CI node aligned to release.yml's 24.18.0 pin per maintainer decision, `.DS_Store` ignored, explicit no-permit-crafting scope line). 034 activates the last deferred direction finding: `BalanceDelta.byCall` per-call attribution via `balanceCheckpoints` (replacing the endpoint before/after arrays in the contract, reusing the generic fill-forward helper), always-on with no opt-in flag, shipped as a 0.x minor via changeset. Independent of each other.

- 035 (2026-07-04, from a cold first-time-consumer README review): the README is ACCURATE post-031/034 — not a rewrite. Three orientation additions (mental-model paragraph, ~25-line API-at-a-glance block derived from the live interface, TOC) plus the one real mismatch found (Getting-started output lacks the required `byCall` field) and a "V1" wording nit. Folded into the 0.2.0 changelog because it landed before the release branch published. Rejected within it: a full rewrite (Known-limitations and examples are battle-tested; rewriting risks reintroducing drift) and a docs-site/API-reference restructure (plan-022 JSDoc serves the reference role; revisit only past ~8 public methods).

- 036 (2026-07-04, Origami consumer blocker: native zap-in routes fail when the wallet lacks ETH): exposes viem's protocol-level `balance` state override as `SimulateArgs.nativeBalanceOverrides` (any account, no discovery/verification, never `unresolved`), plumbed through the existing dormant `extraStateOverrides` seam; `estimateRequirements`'s measurement sim auto-forges native for `from` (parallel to its token forging) so broke wallets get a `native` requirement instead of a collapsed run. TS-only — no contract/bytecode change. Minor changeset. README overlap with 035: either order works, second lands rebases; 035's derive-from-live-interface rule picks up the new field automatically if it runs after.

- 037 (2026-07-04, from the publish-2FA discussion): GitHub Environment `npm-publish` with required reviewers + master-only branch policy gates the publish job; npm trusted-publisher config binds the environment claim; release.yml splits into detect/version-pr/publish so ordinary merges never prompt (detect = local vs published version, failing OPEN to a harmless prompt); per-job least-privilege permissions; CODEOWNERS for workflows (documentation now — the "require code-owner review" toggle is a second-maintainer trigger, since a sole owner cannot approve their own PRs). Operator sequencing is load-bearing: environment → merge → npm binding LAST. Known residual recorded: npm cannot pin `workflow_sha`, so workflow-content changes aren't cryptographically bound to approvals — compensating control is review-on-change. Related decisions from that discussion, operator-side, no plan needed: verify GitHub + npm account 2FA (the actual root credentials of the OIDC chain); consider npm "trusted publisher only" publishing access for the package.

- 039-043 (2026-07-10 deep audit at HEAD `8931d7e`, all nine playbook categories, 8 parallel subagents; run non-interactively, so per the improve skill's default the top 5 findings by leverage were planned without a maintainer selection step — reprioritize freely). 039 is the leverage leader: both access-list revert classification (`rpc.ts:129-137`) and the estimator's insufficient-funds fallback (`requirements.ts:152-154`) key off exact English provider strings; drift breaks discovery/estimation on non-geth-worded providers. 040 adds the missing machine check on the 3-copy `SimulationResult` lockstep (`TxSimulator.sol` struct / `simulator.ts` parseAbi / `fakeClient.ts` type) that plan 038 just edited by hand. 041 fixes AGENTS.md actively misdescribing `simulate()` as discovery-running (pre-031 text) plus stale module lists and small README gaps. 042 closes five verified coverage gaps (console/env debug sink, historical-block params, estimator revert fields + errorAbi, selectorless reverts, one vacuous assertion). 043 batches seven S-size verified items (parallel prepares, one-word probe parse, alias/dead exports, truthful viem peer floor `^2.8.0`, tested TS floor, CI pnpm cache). Ordering: 039 before 042 and 043 (same files — `test/errors.test.ts`, `requirements.ts`); 040 and 041 independent.

## Deferred findings

From the 2026-07-02 deep audit — direction options awaiting a maintainer decision, not planned:

- ~~Per-call attribution API~~ → **activated as plan 034** (2026-07-04, external demand signal from the Origami integration feedback).
- ~~Slot-cache interface~~ → **REJECTED** (maintainer decision 2026-07-04): post-026/031 discovery outputs are plain serializable facts (`TokenSlotOverride[]`), so caller-side caching is a trivial Map with app-specific invalidation (block-trust vs proxy-upgrade lifetimes differ per app); a built-in cache adds interface/invalidation/keying complexity the library shouldn't carry. The library stays pure/stateless; README's "reusable per token/owner/spender for the block/state you trust" is the full caching contract.
- ~~Permit-flow simulation helper~~ → **REJECTED** (maintainer decision 2026-07-04): out of scope permanently; plan 033 adds the explicit "bring signed calldata — the library does not craft or sign permits" line to the README scope docs.

From the 2026-07-10 deep audit — awaiting a maintainer decision, not planned:

- **Fan-out caps / concurrency bound on RPC-driven candidate sets** (SECURITY-01): an adversarial/quirky provider can return huge access lists or forged `observedTokens`, and `estimateRequirements` materializes the tokens×spenders cross product through unbounded `Promise.all` (`requirements.ts:68-101`, `slots.ts:32-44,98-123`). Real hardening, but MED confidence on realization and the caps/threshold values are a product decision (a cap that truncates a legitimately huge batch silently degrades previews). Decide limits, then plan.
- **Vitest 2.x → 4.x major bump** (DEPS-01 + the `pnpm audit` advisories, incl. one critical, all confined to the dev-only vitest>vite path; nothing ships to consumers): M effort, zero consumer risk; also unblocks coverage instrumentation (TESTS-09). Dependabot npm-monthly will keep nudging; do it as one deliberate hop when convenient.
- **Direction options** (maintainer strategy, evidence in the audit report): allowance-delta observation on `simulate()` (the contract already fills `allowanceCheckpoints` on every run; the public surface exposes no way to ask — nearest-rejected-neighbor is plan 003/031's diagnostics, so confirm intent first); ERC-1155/token-id balance probes (README Known-limitations gap; contract change + bytecode regen; no demand signal yet); forged-state discovery for `balanceQueries.forUser` (README:261's own "reverting dry run under-discovers" limitation, hits the view-only-account flagship case).

## Findings considered and rejected

From the 2026-07-10 deep audit (so they aren't re-reported next run):

- **"CLAUDE.md says dist/ is committed" contradiction** — already fixed on HEAD (`8931d7e` review fixes); both agent docs correctly say gitignored + rebuilt at publish.
- **`formatReason` renders `"undefined"` for arg-less `Error` reverts** (`simulator.ts:242-246`) — likely unreachable: viem's `decodeErrorResult` either decodes `Error(string)` with a string arg or throws (caught → selector-only path). Not worth code without a reproduction.
- **`increaseAllowance`-style grants unrecognized by the in-batch allowance detector** (`requirements.ts:19-22`) — real but conservative-direction only (over-estimates, docs already say "pad"), and the selector is nonstandard post-OZ5. Revisit on a consumer report.
- **Sanitizing provider/contract text in error messages and `revertReason`** (SECURITY-04) — library propagates untrusted text verbatim; mitigation is a documented consumer responsibility (README line ships in plan 041) rather than lossy in-library sanitization.
- **`tokenSlotOverridesToStateDiff`/`buildStateOverride` double merge** (`simulator.ts:260-313`) — behavior-neutral refactor on the hot simulation path; MED risk for zero observable gain.
- **Fixture-deploy helper dedup** (~40 `deploy(ctx, "TestToken.sol", ...)` sites) — inline deploys read better per-test; the generic harness (`test/helpers/contracts.ts`) is the right altitude.
- **Bespoke fixture for the `computeAllowanceSlot` re-probe fallback** (`slots.ts:184-185`, unreachable by current fixtures) — M-effort Solidity fixture for a defensive fallback; deferred, not rejected on principle.
- **Duplicate/aliased public-method inputs unspecified** (duplicate balanceQueries/tokens/pairs) — worth a spec eventually; below this run's cut line.
- **Coverage instrumentation** (`@vitest/coverage-v8`) — blocked on the vitest major decision above; adding the v2-compatible package now is double work.
- **Dead fixture `contracts/test/IERC4626.sol`** — verified unreferenced (no `.sol` import, no test deploys it); a one-command deletion left to the maintainer, below plan granularity.
- **oxlint single-rule config** — possibly deliberate minimalism; investigate `categories.correctness` at leisure, no plan.
- **Pre-commit hooks** — rejected: CI is the gate and the full local gate needs Foundry+Anvil; hooks would mostly add friction.
- **foundry-toolchain action v1.8.0 → v1.9.0** — real but Dependabot (github-actions, weekly) surfaces it within days; no plan needed.
- **TypeScript 7 migration** — investigate-only; 5.x fully supported, native-compiler adoption still early.
- **`getCallData` string branch possibly dead** (`data.ts:31`) — LOW-confidence smell; confirm `client.call`'s return shape under a custom transport before touching.
- **Parallelizing per-storage-key slot verification** (`probes.ts:78-91`) — the sequential early-exit IS the RPC-count optimization; access lists typically carry 1-2 keys; a parallel race would break pinned counts.
- **Foundry nightly pin** — re-verified still required: latest stable v1.7.1 (2026-05-08) predates foundry#14569 (merged 2026-05-23). The ci.yml comment remains accurate.
- **`@types/node@20` vs Node 24 CI** — correct: a library types against its `engines` floor, not CI's runtime.
- **`typescript@5.6.1-rc` in the lockfile** — vendored by `@arethetypeswrong/core`; intentional, not project TS.

From the 2026-07-02 deep audit (so they aren't re-reported next run):

- **"viem 2.53+ requires TypeScript 5.9"** — false; viem 2.54.0's peer dep is `typescript >=5.0.4` (checked node_modules/viem/package.json).
- **"Redundant access lists between candidate discovery and slot probing"** — misread; discovery access-lists the user's calls, probes access-list `balanceOf`/`allowance` calldata. Different queries, no redundancy.
- **ERC-1271 accepts `address(0)` recoveries** — impossible: the comparison is against `address(this)`, which is never zero.
- **High-s signature malleability in `_recover`** — deliberate mirror of raw `ecrecover` (what Permit2 does for EOAs); rejecting high-s would create the opposite simulation/reality divergence. Wallet signatures are canonical regardless.
- **10^50 sentinel detectable by malicious contracts** — inherent to forged-state simulation (injected code at the user's address is a bigger tell than the sentinel); randomization wouldn't remove detectability. Documented posture, not a fix.
- **Caller-supplied `tokenSlotOverrides` "let a dapp forge fake previews"** — trust-boundary confusion: `simulate()` args come from the wallet (the trusted caller), not the dapp.
- **Unbounded revert-reason string** — bounded in practice by RPC response caps and viem's decoder; not worth code.
- **Contract gas exhaustion under many candidates/probes** — speculative at 16M default with small real-world widths; noted as a maintenance concern only.
- **Layout-inference fallback untested** — it is tested: "falls back for non-standard allowance slots" pins 2 access-list probes and exact amounts.
- **Brittle debug-step-count assertions** — deliberate: pinning exact RPC counts is the library's performance regression net.
- **Mainnet tests skip silently without `MAINNET_RPC_URL`** — by-design opt-in.
- **`.editorconfig` missing** — marginal for a repo already formatter-enforced via oxfmt; skipped.

From the 2026-07-02 architecture/DX-focused audit (016-022 wave):

- **`AssetBalanceDelta` as a discriminated union / "native" typo footgun** — the union members would differ in no other field, so a discriminated union adds nothing; TS already rejects comparisons against non-overlapping literals like `"Native"`.
- **`probeData` computed when `allowanceProbes` is empty** — one tuple decode of empty arrays; not worth an API flag.
- **`parseAbi` at module load** — standard viem usage, one-time cost.
- **`mappingSlot` "underutilized export"** — it is the building block of `allowanceSlotFor`; working as intended.
- **`Req`/`Resp` type-name suffixes and `find*` method renames** (from the instance-API sketch) — rejected in that wave to avoid restructuring and renaming at once. Later superseded by plan 027's `prepare*Overrides` / `estimateAssetRequirements` rename.
- **Binding `from` into the instance API** — rejected: wallets switch accounts; cache keys include owner anyway.

Positive finding worth preserving (added to CLAUDE.md by plan 022): internal modules never import from public modules; no cycles.

## Reconciliation log

- **2026-07-11 (HEAD `0a1e76e`)**: reconcile pass over the 039–043 batch after the post-plan review commit `0a1e76e` (review cuts touching 039's `rpc.ts` and 040's `abi.test.ts`). All five re-spot-checked on HEAD and still hold: **039** — no exact-string classifiers remain in `rpc.ts`/`requirements.ts` (grep clean), `hasRevertCode` present; the review commit dropped a redundant object pre-guard in `isRpcExecutionRevert` (behavior identical — `hasRevertCode` self-guards); **040** — `test/abi.test.ts` present and passing; the review commit folded `stripInternalType` into `normalizeFn` and removed a redundant JSON deep-clone (comparison surface unchanged); **041** — old method name absent from AGENTS.md, `checkpoints.ts`/`debugSteps.ts` in both module lists, Foundry nightly string byte-identical across README and ci.yml; **042** — `toBeDefined`=0, debug-sink and historical-block tests present; **043** — aliases/`AccessListEntry` grep clean, `viem: ^2.8.0`, `cache: pnpm` in ci.yml only. Gate on HEAD: `pnpm verify` exit 0 (71 passed / 4 mainnet skipped), run post-review-commit. **Four pending changesets** (slim-ghost-contract-delta, remove-debug-rpc-env, classify-provider-errors, hygiene-sweep-parallel-prepares — all patch) await the next push → Version Packages PR → 0.2.2; master is ahead of origin, nothing pushed yet. Backlog: **43/43 DONE, zero TODO/BLOCKED.** Executable right now: nothing in the ledger; open options are the three direction findings (allowance-delta observation on simulate, ERC-1155 id probes, forged-state discovery) and the deferred audit items (fan-out caps, vitest major bump + coverage instrumentation, oxlint categories) recorded in docs/audit-2026-07-10.md.

- **2026-07-11**: plan 043 verified DONE, executed directly on master per maintainer instruction (no branch). Drift check reconciled: `git diff --stat 8931d7e..HEAD` over the tracked files showed only the expected 039 changes (requirements.ts +1, rpc.ts +17); every "Current state" excerpt still matched byte-for-byte at the shifted line numbers, so no STOP fired. Per-item disposition, all seven applied as specified: **(1) parallel prepares** — the two sequential `await`s in `requirements.ts` (balance + allowance override preparation) wrapped in one `Promise.all` with byte-identical args and destructured in original order; the pinned per-operation RPC-count/delta tests in `test/requirements.test.ts` passed unchanged, confirming the two fan-outs were genuinely independent (STOP not triggered). **(2) strict word parse** — `readUint256Call` in `probes.ts` now `BigInt(data.slice(0, 66))` (one 32-byte word); the `< 66` guard stays; existing sentinel-verification suites and the short-returndata test passed unchanged (STOP not triggered). **(3) alias removal** — deleted `estimateTokenOverrideRequirements` (requirements.ts) and `prepareBalanceTokenOverrides`/`prepareAllowanceTokenOverrides` (slots.ts); `txSimulator.ts` imports/call sites switched to canonical `estimateAssetRequirements`, `prepareBalanceOverrides`, `prepareAllowanceOverrides`; pre-edit grep confirmed the only importer was `txSimulator.ts` (no test/README refs), post-edit grep is clean. **(4) dead rpc surface** — removed unused `export type AccessListEntry`; `AccessList` import retained (still used by `AccessListRpcResult` and `createAccessList`'s return); `emitDebug` de-exported to file-local (typecheck clean, no external callers). **(5) honest floors** — `peerDependencies.viem` `2.x`→`^2.8.0` (StateOverride type first re-exported at 2.8.0), `devDependencies.typescript` `^5.5.4`→`^5.9.0` (the floor CI's smoke test actually exercises); `pnpm install` refreshed only the `typescript` specifier line in the lockfile; **`dist/` type output unchanged** (`git status -- dist/` empty), so the floor-bump STOP did not fire. **(6) CI pnpm cache** — moved `corepack enable` above setup-node and added `cache: pnpm` in `ci.yml` only; `release.yml` untouched. **(7) changeset** — one patch changeset (`.changeset/hygiene-sweep-parallel-prepares.md`) with the plan's suggested text. Deviation from git workflow: no branch/PR (maintainer instruction to work on master); `pnpm changeset` is interactive so the changeset file was written directly in the repo's established format. Done criteria all hold: `pnpm verify` exit 0 (71 passed / 4 mainnet skipped — unchanged from 042, as intended); alias/`AccessListEntry` grep no matches; `"viem": "^2.8.0"` present; `cache: pnpm`=1 in ci.yml, `cache`=0 in release.yml (release.yml diff empty); `attw --pack . --profile esm-only` exit 0; new patch changeset present; no out-of-scope files modified. Backlog: 43/43 DONE; zero TODO/BLOCKED.

- **2026-07-11 (HEAD `a4a5518`)**: plan 042 verified DONE on HEAD (executed directly on master per maintainer instruction — no branch). Drift check clean: `git diff --stat 8931d7e..HEAD -- test/ src/internal/rpc.ts src/internal/requirements.ts` showed only the expected plans 039/040 changes (rpc.ts +17, requirements.ts +1, test/abi.test.ts new, test/errors.test.ts +76). Line numbers had shifted but all named gaps were still open. Test-only: **no `src/` or `contracts/` file touched** (verified via `git status`); the STOP condition "any new test fails against unmodified production code" did NOT fire — every new test passed against production as-is. Per-gap disposition, all five closed as specified: **(1) debug sink** — two tests in `test/errors.test.ts` (`debug: true` and `VIEM_TX_SIM_DEBUG_RPC=1`), each spying `console.debug` and asserting the `[viem-tx-sim]` prefix and the `txSimulator.simulate` step literal (ADR-0001). One method deviation from the plan text: capture `spy.mock.calls` into `logged` BEFORE `spy.mockRestore()` in the try block — `mockRestore()` resets `mock.calls`, so reading after restore (as the plan's finally-then-read sketch implied) yields empty; verified the underlying production sink emits 2 calls via an out-of-vitest repro before adjusting. **(2) selectorless revert** — one `test/errors.test.ts` test scripting `revertData: "0x"`, asserting `status === "reverted"`, `revertData === "0x"`, `revertSelector`/`revertReason`/`revertError` all undefined, `failingCallIndex === 0`. **(3) historical block** — one `test/simulate.test.ts` Anvil test (mint 1000, pin block, mint 500, simulate a 1n transfer at `blockNumber: pinned`, assert `before === 1_000n` + `delta === -1n`); the state-override-at-historical-block STOP condition did not fire. **(4) estimator revert fields + errorAbi** — one `test/requirements.test.ts` test crossing the reverting-prefix exemplar with the custom-error decode exemplar: `estimateRequirements` on `CustomErrorTarget.failWithArgs(1,2)` with `errorAbi`, asserting `failingCallIndex === 0`, `revertError === { name: "InsufficientBalance", args: [1n,2n] }`, `revertReason === "InsufficientBalance(1, 2)"`, `revertSelector` defined. **(5) vacuous test** — replaced `expect(fake).toBeDefined()` with `await expect(fake.simulate(<minimal args>)).resolves.toEqual(<stub return>)`; the module-scope `fake: TxSimulator` type-conformance const is unchanged (compile-time check preserved). All done-criteria greps pass (`debug: true`≥1, `VIEM_TX_SIM_DEBUG_RPC`≥1, `blockNumber`≥1, `errorAbi`≥1, `toBeDefined`=0). Gate: `pnpm verify` exit 0 (71 passed / 4 mainnet skipped — up exactly 5 from 66, Step 5 replacing 1:1). No changeset (test-only). Deferred per plan: TESTS-07 (duplicate-input aliasing), TESTS-03 (`computeAllowanceSlot` re-probe fixture), TESTS-09 (coverage instrumentation, blocked on vitest major). Backlog: 41/41 DONE; 043 remains TODO.

- **2026-07-11 (HEAD `260887c`)**: plan 041 verified DONE on HEAD. Docs-only sync of AGENTS.md/CLAUDE.md/README against the shipped architecture. Per-item disposition: **AGENTS.md architecture** — applied (replaced the pre-031 "candidate discovery runs eth_createAccessList … simulation then performs one eth_call" text with the current CLAUDE.md framing: `simulate()` takes explicit `balanceQueries`, one `eth_call`, must not run access-list discovery; `balanceQueries.forUser`/`discoverErc20s` own discovery; queried reads can target any account); **AGENTS.md method name** — applied (`estimateAssetRequirements()` → `tokenOverrides.estimateRequirements()`, verified at `txSimulator.ts:122`); **AGENTS.md checkpoint wording** — applied (`allowanceCheckpoints[...]` → generic `checkpoints[probeIndex * (calls.length + 1) + callIndex]` covering both grids); **AGENTS.md + CLAUDE.md module lists** — applied (added `checkpoints.ts`, `debugSteps.ts`; `queryDiscovery.ts` to AGENTS only, already in CLAUDE — one-liners lifted from the source headers verbatim); **README revertSelector** — **already-done** (line 114 already carried `revertSelector?` from the 917154d/e712c58 rewrite that post-dated the plan's `8931d7e` stamp; grep confirmed 1 occurrence before any edit); **README Foundry nightly + single-file loop** — **adapted** (plan's line numbers were stale after the rewrite, so integrated into the researched Development section rather than appended: pinned `nightly-7debd6d47628c5551837534aee507dbf552d5889` note with `foundryup --install` command byte-identical to `ci.yml:25`, and the `pnpm build:contracts && pnpm exec vitest run test/simulate.test.ts` inner-loop line — both target files verified to exist); **README untrusted-text sentence** — applied (added to Decoding reverts, discharging the SECURITY-04 residual noted at line 115). No STOP condition fired: CLAUDE.md's architecture framing matched source, and no factual drift beyond the listed items surfaced. Out of scope untouched (CONTEXT.md, ADR-0001, src/test/.github). All Step 1–3 grep gates pass (estimateAssetRequirements→0, module refs 3/2, revertSelector/nightly/untrusted each ≥1); nightly string byte-identical across README and ci.yml. Gate: `pnpm verify` exit 0 (66 passed / 4 mainnet skipped); `pnpm lint` clean (oxfmt does not touch `.md`). No changeset (docs-only). Backlog: 40/40 DONE; 042-043 remain TODO.

- **2026-07-11 (HEAD `260887c`)**: plan 040 verified DONE on HEAD. Drift check clean (`git diff --stat 8931d7e..HEAD` over the three tracked files empty; `txSimulatorAbi` at `simulator.ts:59-67` and `artifacts.ts` both byte-identical to the plan's "Current state" excerpts). Added `test/abi.test.ts` (test-only, in scope; nothing under `src/`, `contracts/`, or `test/helpers/` touched). One authorized normalization beyond `stripInternalType`: the forge artifact labels `simulate`'s output tuple `name: "result"` (Solidity's named return var) while viem's `parseAbi` omits the name for the unnamed `returns (SimulationResult)` — a representational-only diff explicitly sanctioned by Step 1. Fix is a narrow `normalizeFn` that drops ONLY the top-level output-parameter `name`; `stripInternalType` still strips only `internalType`, and every input name plus every SimulationResult struct-component name stays compared, so real field name/type/order/component drift still fails (proven in Step 2: renaming `balanceProbeOk`→`balanceProbeOkX` in the parseAbi string failed the test; reverted via `git checkout`, leaving only `test/abi.test.ts` untracked). No STOP condition fired (no real signature mismatch). No changeset (test-only, no published behavior change). Gate: `pnpm verify` exit 0 (66 passed / 4 mainnet skipped — up one with the new guard). Backlog: 39/39 DONE; 041-043 remain TODO.

- **2026-07-10 (HEAD `e712c58`)**: plan 039 verified DONE on HEAD. Drift check clean (no `src/`-or-`test/` changes since the planned-at `8931d7e`; only README/audit-artifact commits intervened). Both string classifiers now key off structured signals: `rpc.ts` classifies an access-list revert when any object on the cause chain carries JSON-RPC `code === 3` OR the message matches `/revert/i` (result-shaped and thrown paths both), replacing the redundant `execution reverted|Execution reverted` double-alternative; `requirements.ts` widens the unfunded fallback to `/insufficient (funds|balance)/i`, catching geth's lowercase "insufficient funds for gas * price + value". Five new `fakeClient` tests in `test/errors.test.ts` pin both directions: alternate revert wording resolves, a `code: 3` error with non-revert prose ("VM execution error") resolves (viem preserves `code` on the cause chain — the STOP condition did not fire), the lowercase insufficient-funds fallback runs (asserted via a `txSimulator.simulate` start event with `details.candidates === 1`), and two negative controls ("connection refused") still throw `AccessListUnsupportedError` from both `forUser` and `estimateRequirements`. Patch changeset `classify-provider-errors.md` added. Gates: `pnpm typecheck` + `pnpm lint` green; full suite green (65 passed / 4 mainnet skipped) — one Anvil spawn-timeout flake on the parallel `pnpm verify` run cleared on isolated + full re-run. No files outside the in-scope three (+ changeset) touched; no RPC count or debug-step literal changed. Deferred per plan: provider-text sanitization (SECURITY-04) ships in plan 041. Backlog: 38/38 DONE.

- **2026-07-10 (HEAD `32046df`)**: plan 037 verified DONE on HEAD (PR #9 merged). Criteria: `release.yml` has the three jobs (detect/version-pr/publish) with `environment: npm-publish` exactly once and `id-token` exactly once (publish job only), zero mutable `@vN` action refs; `.github/CODEOWNERS` carries both owner lines; CLAUDE.md documents the gated flow and the solo-maintainer Code-Owners caveat; zero pending changesets; detect logic was tested in both directions by the executor and `pnpm verify` exited 0 (51 passed / 4 mainnet skipped) on the identical pre-merge tree. **Operator sequencing completed in order**: `npm-publish` environment created (required reviewer `frontier159`, master-only, self-review allowed), PR #9 merged, npm trusted-publisher environment binding saved LAST. **Known residual: the pipeline is unproven until the first gated release** — expect exactly one "Review deployments" prompt on the next Version Packages merge; keep that release small. Also reconciled from the intervening release window: **0.2.1 published** (PR #8, consuming the `native-balance-overrides` patch changeset); plan 036's file was amended in place (minor→patch) to match the shipped release type; unplanned fix `ae60b15` ("Preserve direct candidates on fallback") shipped in 0.2.1 — `estimateAssetRequirements`'s insufficient-funds fallback now retains direct call targets as candidates instead of an empty list (requirements.ts:53); no plan criteria affected, suite green on the tree containing it. Backlog: **37/37 DONE, zero TODO/BLOCKED, zero deferred findings.**

- **2026-07-04 (HEAD `167ae9e`)**: plans 035 + 036 verified DONE on HEAD; **0.2.0 released** (Version Packages PR #6 merged — byCall attribution shipped). Gates: typecheck + lint + attw green; suite green twice (51 passed / 4 mainnet skipped — up 4 with the native-override tests). 035: rode the 0.2.0 version branch per its amended scope (README note folded into CHANGELOG); at-a-glance block + mental model present, byCall in examples, no "V1" vocabulary, TOC in place. 036: `NativeBalanceOverride` exported, optional `nativeBalanceOverrides` on SimulateArgs plumbed via `extraStateOverrides` (txSimulator.ts:203), `estimateRequirements` measurement forges native for `from` exactly as specified (requirements.ts:100), broke-wallet test present with its "fails pre-036" marker, contracts/generated untouched, patch changeset pending (`native-balance-overrides` → next release is 0.2.1). Plan amendment honored: the native max-uint guard was deliberately DROPPED pre-execution (the decrement-blindness rationale is ERC-20-specific and does not transfer to native balances) — verified no guard exists and a positive max-native override test exists, consistent with the amended plan text. The 035 same-PR rule proved itself immediately: 036's executor updated the day-old at-a-glance block and mental-model paragraph with the new field in the same PR. Backlog: 36/36 DONE.

- **2026-07-04 (HEAD `f7ad02a`)**: plans 033 + 034 verified DONE on HEAD. Gates: typecheck + lint green; focused suite green three consecutive runs plus `pnpm verify` (47 passed / 4 mainnet skipped — up 3 with the attribution tests). 033: both workflows report the identical `node-version: 24.18.0`, `.DS_Store` ignored, permit-scope sentence present in README. 034: contract carries `balanceCheckpoints` with zero `balanceBefore`/`balanceAfter` remnants; `BalanceDelta.byCall` required in types; attribution coverage present across simulate tests, README, and CLAUDE.md; `test/requirements.test.ts` untouched; a **minor** changeset (`fuzzy-balance-checkpoints`) correctly describes the break — merging the next Version Packages PR ships it as 0.2.0. **Backlog: 34/34 DONE, zero TODO/BLOCKED, zero deferred findings** — the ledger is fully clear for the first time; future work enters via new audits or maintainer/consumer requests.

- **2026-07-03 (HEAD `9f86857`)**: plans 031 + 032 verified DONE on HEAD. Gates: typecheck + lint green; suite green twice consecutively (44 passed / 4 mainnet skipped); attw clean; `dist/` exports the new surface (no `AssetBalanceDelta`, `TxSimulator.create` present). 031: contract has `BalanceProbe` + fourth param + `_snapshotBalanceProbes`; `SimulateArgs.balanceQueries` required; `BalanceQuery`/`BalanceDelta` exported; `discoverCandidateAddresses` referenced only from `internal/` (simulate is discovery-free); the candidates-observation-only invariant re-verified by grep. 032: interface is exactly `simulate` + `balanceQueries.{forUser,discoverErc20s}` + `tokenOverrides.{forBalances,forAllowances,estimateRequirements}`; no flat method paths remain outside arg/result type names; full `EstimatedAssetRequirements` return preserved. Note: external PR #4 merged (`a754da6`, pins release workflow node to 24.18.0) — first non-plan contribution; release.yml now on node 24 while ci.yml verify remains node 22, an intentional-looking pin but worth the operator confirming the mismatch is deliberate. Untracked `.DS_Store` in tree (add to .gitignore at leisure). Backlog: 32/32 DONE.

- **2026-07-03 (HEAD `1878564`)**: plans 029 + 030 verified DONE on HEAD. Gates: typecheck, lint, suite (40 passed / 4 mainnet skipped), and `pnpm exec attw --pack . --profile esm-only` all green; tree clean. 030: zero `dlx` in workflows, attw is a lockfile-pinned devDependency, `permissions: contents: read` on ci.yml, all action refs 40-char SHAs with version comments and **zero `@vN` refs in either workflow**, `minimumReleaseAge: 4320` with posture comment, smoke install `--ignore-scripts` + exact pins (viem@2.54.1/ts@5.9.3/@types/node@20.19.43), dependabot.yml present. SHA↔tag mappings independently re-verified via `git ls-remote` against the official repos: checkout@v7.0.0, setup-node@v6.4.0, foundry-toolchain@v1.8.0 all genuine (executor pinned newer majors than the plan's v4/v1 examples — a documented, verified improvement, not drift). 029: `.changeset/config.json` (access public, baseBranch **master**), version still 0.1.0 with no stray changesets, release.yml has the fork guard, master trigger, `id-token: write`, provenance env, `publish: pnpm release`, foundry nightly byte-identical to ci.yml, and **zero `NODE_AUTH_TOKEN`** (OIDC-only as decided). CLAUDE.md documents the flow. Outstanding operator TODOs (not executor items): configure npm Trusted Publishing for `release.yml`, confirm `viem-tx-sim` name availability on npm. Backlog: 30/30 DONE.

- **2026-07-03 (HEAD `b3390e0`)**: plan 027 (externally authored — intent-centric renames: `prepare*Overrides`/`estimateAssetRequirements`) verified DONE on HEAD. The reconcile initially raced the in-flight execution (staged-only snapshot); after commit `b3390e0` landed: zero matches for any old `discover*`/`Discovered*` public name in src/test/README, all 8 new type names exported from index.ts, `dist/` `.d.ts` files carry only the new surface, typecheck + lint green, suite green (40 passed / 4 skipped — identical count to pre-rename, confirming pure rename). One benign uncommitted leftover noted: a `forge-lint: disable-next-line(custom-errors)` comment on the intentional string-revert fixture in `contracts/test/CustomErrorTarget.sol` (comment-only; maintainer to commit or drop at will). Backlog: 27/27 DONE.

- **2026-07-03 (HEAD `ab56dab`)**: plan 026 verified DONE on HEAD. Gates: typecheck + lint green; suite green (40 passed — the new max-amount guard test added — 4 mainnet opt-ins skipped). Criteria: `TokenSlotOverride.amount` required (no optional occurrence), `BalanceSlot` gone outside `BalanceSlotDiscovery`, `AllowanceSlot` declared as `TokenSlotOverride &` intersection, no `?? OVERRIDE_TOKEN_AMOUNT` fallback remains, max-uint guard at simulator.ts:290 with an `InvalidSimulationInputError` test, deal-framed docs in types.ts (4 mentions) and README. Backlog fully executed: 26/26 DONE; open items remain the three deferred direction findings.

- **2026-07-03 (HEAD `9b598c5`)**: plans 023-025 verified DONE on HEAD. Gates: typecheck + lint green; suite green twice (39 passed — 024's four revert tests added — 4 mainnet opt-ins skipped); `dist/` exports instance-only surface. Per-plan: no `Omit`/`Parameters`/`ReturnType` gymnastics in txSimulator.ts, `client` exists only in `TxSimulatorConfig`, the four bound-arg type names exported (023); `decodeRevertReason` gone, `RevertError` exported, `errorAbi` on exactly the three specified types, custom-error tests present (024); top level is exactly the 5 public files, `internal/` exactly the 6 domains (data/probes/requirements/rpc/simulator/slots), zero `StorageOverride` references, gas default at the single `defaults()` site, CLAUDE.md refs current (025). Backlog fully executed: 25/25 DONE. Open items: the three deferred direction findings only.

- **2026-07-03 (HEAD `60d9140`)**: plans 016-022 verified DONE on HEAD. Gates: typecheck + lint green; suite green (35 passed, 4 mainnet opt-ins skipped); built `dist/` exports exactly the instance-only surface — `TxSimulator.create` present, `simulate`/`discover*` absent, both constants exported with correct values. Per-plan: no `calldata` field remains (the one grep hit is the word inside the `data` field's JSDoc — fine); `internal/allowanceDiscovery.ts` exists and the private requirements twin + helpers are gone (017); no optional `revertData` in types, success/reverted variant types exported (018); `unresolved` threaded through slots/types/requirements (019); `RpcCallArgs` in rpc.ts and the decode `as unknown` cast is GONE — viem inference worked (020); `test/txSimulator.test.ts` present, index.ts instance-only (021); `src/constants.ts` single-sources both defaults, JSDoc counts healthy across types/errors/txSimulator (022). Backlog fully executed; open items remain the three deferred direction findings (per-call attribution API, slot cache — now homed in `create()`, permit helper).

- **2026-07-02 evening (HEAD `ed0031a`)**: plans 008-015 verified DONE on HEAD. `pnpm audit --prod` clean (010); typecheck + lint green; suite green (28 passed across simulate/requirements/errors, 4 mainnet opt-ins skipped). Per-plan checks: CI workflow with pinned foundry nightly + dist-freshness gate + 2× `process.kill` (008); permit detection + outflow clamp + PermitToken/PermitRelayer fixtures + 10 requirements tests (009); `undecodable` decode wrap + 6 error tests (011); single `buildCallParameters` in rpc.ts, no stray block-option spreads, no `eth_getCode` (012); `Promise.all` in all three loops, no `allSettled` (013); CLAUDE.md + fixed README link + historical note (014); 3 receiver hooks + MockERC721 + NFT-receipt test + README bullet flipped (015). Notes:
  - `test/docs.test.ts` deletion + `docs/motivation.md` restructure (numbered post headings removed) in `8d9a02e`: **confirmed intentional by the maintainer** (2026-07-02). Content and all six image assets survive; nothing automated guards the motivation doc anymore, by choice. Plan 005's docs criterion is fully retired. Do not re-flag.
  - Backlog fully executed again; deferred direction findings (per-call attribution API, slot cache, permit helper) remain the open options.

- **2026-07-02 (HEAD `7f94c6f`)**: all seven DONE plans spot-checked and verified — typecheck, lint, and the anvil test suite (19 passed, 4 skipped mainnet opt-ins) pass on HEAD; per-plan greps and export checks hold; `dist/` is fresh and exports the plan-007 API. Notes:
  - Plans 002/004 criteria referencing `spender`/`currentAllowance` attribution tests are superseded by 006, which removed attribution entirely; the surviving `requiredAllowances()` identifier in `src/requirements.ts` is a plan-007 helper, unrelated to the field 002 removed.
  - Plan 005's `pnpm check:docs` criterion is unrunnable as written; `files` in `package.json` still satisfies both 005 packaging checks.
  - Backlog is fully executed; nothing TODO/BLOCKED/IN PROGRESS.
