# Deep audit findings — 2026-07-10

Full-repo `/improve deep` audit at HEAD `8931d7e` (pre-README-rewrite). Eight parallel read-only auditors covered all nine categories; every finding below was re-verified against the cited code before inclusion. Plans referenced are in `plans/` (039–043), index in `plans/README.md`.

Effort: S (hours) / M (day-ish) / L (multi-day). Confidence: HIGH (code read, certain) / MED (needs verification) / LOW (investigate).

## Correctness / bugs

| Finding | Evidence | Impact | Effort | Risk | Conf | Disposition |
|---|---|---|---|---|---|---|
| Revert + insufficient-funds classification hinges on exact English provider strings; wording drift turns benign reverts into `AccessListUnsupportedError`, killing discovery/estimation on non-geth-worded providers | `src/internal/rpc.ts:129-137`, `src/internal/requirements.ts:152-154` | Wallet previews break or under-report on provider drift | M | MED | MED-HIGH | **Plan 039** |
| `readUint256Call` parses full returndata with `BigInt`; >32-byte responses from non-standard tokens cause false-negative slot discovery | `src/internal/probes.ts:175-177` | Reduced override coverage for quirky tokens (fails safe) | S | LOW | HIGH | **Plan 043** |
| In-batch allowance detector recognizes only `approve`/`permit`, not `increaseAllowance` | `src/internal/requirements.ts:19-22` | Over-estimates only (conservative; docs say "pad") | S | LOW | MED | Rejected — nonstandard selector, safe direction |
| `formatReason` could render `"undefined"` for arg-less `Error` reverts | `src/internal/simulator.ts:242-246` | Cosmetic | S | LOW | LOW | Rejected — likely unreachable via viem's decoder |

## Security

| Finding | Evidence | Impact | Effort | Risk | Conf | Disposition |
|---|---|---|---|---|---|---|
| Unbounded fan-out from RPC-controlled candidate sets (tokens×spenders cross product, uncapped `Promise.all`) | `src/internal/simulator.ts:197-217`, `requirements.ts:68-87`, `slots.ts:32-44` | Memory blowup / RPC amplification under adversarial provider | M | LOW | MED | Deferred — caps are a maintainer threshold decision |
| Untrusted provider/contract text propagated verbatim into error messages and `revertReason` | `src/internal/rpc.ts:124-127`, `simulator.ts:242-246` | Potential text injection into wallet UI | S | LOW | LOW | Docs mitigation in **Plan 041**; code sanitization rejected |

(The provider-string classification finding above is also security-relevant: an adversarial provider can steer classification.)

## Performance

| Finding | Evidence | Impact | Effort | Risk | Conf | Disposition |
|---|---|---|---|---|---|---|
| `estimateRequirements` serializes two independent RPC fan-outs | `src/internal/requirements.ts:72-87` | One avoidable full round-trip per estimate | S | LOW | HIGH | **Plan 043** |
| CI has no pnpm store cache; cold install every run | `.github/workflows/ci.yml:15-26` | Fixed wall-time overhead per push/PR | S | LOW | HIGH | **Plan 043** |
| Sequential per-storage-key slot verification | `src/internal/probes.ts:78-91` | N serial round-trips worst case (typically 1–2 keys) | M | MED | LOW | Rejected — early-exit IS the RPC-count optimization |

## Test coverage

| Finding | Evidence | Impact | Effort | Risk | Conf | Disposition |
|---|---|---|---|---|---|---|
| Five coverage gaps: console/env debug sink, `blockNumber`/`blockTag` never run in CI, estimator revert fields + `errorAbi` unasserted, selectorless reverts unpinned, one vacuous test | `rpc.ts:154-209,34-40`, `requirements.ts:133-145`, `txSimulator.test.ts:86-88` | Documented behavior can regress silently | S-M | LOW | HIGH | **Plan 042** |
| `computeAllowanceSlot` re-probe fallback unreachable by any fixture | `src/internal/slots.ts:184-185` | Defensive branch unverified | M | LOW | HIGH | Deferred — bespoke fixture not worth it now |
| Duplicate/aliased inputs to public methods unspecified | `checkpoints.ts:29-64`, `slots.ts:95-138` | Refactors could silently change aliasing behavior | S | LOW | MED | Deferred |
| No coverage instrumentation | `package.json` scripts | Gaps only findable by reading | S | LOW | HIGH | Deferred — blocked on vitest bump |

## Tech debt & architecture

| Finding | Evidence | Impact | Effort | Risk | Conf | Disposition |
|---|---|---|---|---|---|---|
| `SimulationResult` shape hand-maintained in 3 places (Solidity struct / `parseAbi` string / fakeClient type) with zero guard; drift surfaces as "your RPC is broken" | `contracts/TxSimulator.sol:28-38`, `simulator.ts:60-67`, `test/helpers/fakeClient.ts:33-43` | Silent mis-decode on next contract change (plan 038 just did this edit by hand) | S | LOW | HIGH | **Plan 040** |
| Alias exports (`*TokenOverrides`, `estimateTokenOverrideRequirements`) + dead `AccessListEntry`, over-exported `emitDebug` | `slots.ts:198-199`, `requirements.ts:150`, `rpc.ts:65,154` | Navigation noise, phantom API | S | LOW | HIGH | **Plan 043** |
| Dead 12KB fixture `contracts/test/IERC4626.sol`, never imported or deployed | grep-verified | Dead compile + misleading surface | S | LOW | HIGH | Recorded; one-command delete, below plan granularity |
| `getCallData` string branch may be dead | `src/internal/data.ts:30-36` | Untestable defensive code | S | LOW | LOW | Recorded — investigate |
| `tokenSlotOverridesToStateDiff` duplicates `buildStateOverride`'s merge | `src/internal/simulator.ts:260-313` | Two copies of merge semantics | S | MED | MED | Rejected — hot-path churn, zero observable gain |

## Dependencies & migrations

| Finding | Evidence | Impact | Effort | Risk | Conf | Disposition |
|---|---|---|---|---|---|---|
| viem peer `2.x` provably too wide — `StateOverride` type import needs ≥2.8.0 | `package.json:68`, `rpc.ts:8` | False compatibility promise | S | LOW | HIGH | **Plan 043** (`^2.8.0`) |
| Declared TS floor `^5.5.4` tested nowhere; CI smoke pins 5.9.3 and drifts by hand | `package.json:63`, `ci.yml:39` | Untested compat claims | S | LOW | HIGH | **Plan 043** |
| Dev-only advisories (1 critical vitest, 1 high vite, 3 moderate — `pnpm audit`); vitest 2 majors behind | `package.json:65` | Dev/CI machines only; nothing ships | M | LOW | HIGH | Deferred — deliberate 2→4 hop; unblocks coverage tooling |
| foundry-toolchain action v1.8.0 → v1.9.0 (hardening release) | `ci.yml:21`, `release.yml:64` | CI supply-chain surface | S | LOW-MED | MED | Left to weekly Dependabot |
| TypeScript 7 (native compiler) now GA | `package.json:63` | Forward-looking only | L | MED | LOW | Recorded — investigate later |

## DX & tooling

| Finding | Evidence | Impact | Effort | Risk | Conf | Disposition |
|---|---|---|---|---|---|---|
| Foundry nightly pin + single-file test loop undocumented outside CI | `ci.yml:22-25` vs README/CLAUDE.md | Opaque local test failures for contributors | S | LOW | HIGH | Folded into **Plan 041** |
| oxlint config is one rule; recommended categories off | `.oxlintrc.json` | Near-empty lint gate | S | LOW | LOW | Recorded — investigate |
| Pre-commit hooks | — | — | — | — | HIGH | Considered, not worth adding — CI is the gate, local gate needs Foundry |

## Docs

| Finding | Evidence | Impact | Effort | Risk | Conf | Disposition |
|---|---|---|---|---|---|---|
| AGENTS.md describes pre-plan-031 architecture (`simulate()` runs discovery — it must not), names a nonexistent method, omits the invariant-bearing modules (`checkpoints.ts`, `debugSteps.ts`) | `AGENTS.md:12-14,26,50`; Key-modules lists in both agent docs | Agent executors misled about pinned invariants | S | LOW | HIGH | **Plan 041** |
| README at-a-glance omitted `revertSelector?` | `types.ts:279` | Minor inconsistency | S | LOW | HIGH | **Already fixed** by the README rewrite (`917154d`, post-audit-snapshot) |

## Direction (maintainer options — not ranked against bugs, not planned)

1. **Allowance-delta observation on `simulate()`** — the ghost contract already accepts `AllowanceProbe[]` and fills a per-call `allowanceCheckpoints` grid on every run (`TxSimulator.sol:57-69`); the public surface offers no way to ask (`txSimulator.ts:207-210`). Wallets showing "you are granting X" currently need a second estimate pass. TS-only, effort M (coarse). Trade-off: sits near rejected plan-003/031 items — confirm the observation-output axis isn't what was meant to be rejected.
2. **ERC-1155 / token-id balance probes** — the contract can receive 1155/721 safely (`TxSimulator.sol:97-111`) but probes only `balanceOf(address)`; the README states the gap. Effort L (contract change + gated bytecode regen). No in-repo demand signal — check integrators first.
3. **Forged-state discovery for `forUser`** — the README's own limitation: a reverting dry run under-discovers, hitting exactly the view-only/underfunded flagship case; `estimateRequirements` already owns the forging machinery to reuse. Effort M, opt-in/additive.

## Not audited

- Mainnet integration paths (no `MAINNET_RPC_URL` in the audit run).
- Live provider behavior matrix — the classifier findings rest on code reading, not live Erigon/Besu/Nethermind probing.
- The gated publish pipeline in execution (unproven until the first release).
- Generated bytecode ↔ contract correspondence (trusted to CI's freshness gate).
- Runtime performance profiling (perf findings are structural, unmeasured).

## Plans written (all stamp `8931d7e`)

| Plan | Title | Priority | Effort |
|---|---|---|---|
| 039 | Classify provider reverts/insufficient-funds on structured signals | P1 | M |
| 040 | Guard the simulator ABI against forge-artifact drift | P1 | S |
| 041 | Sync AGENTS.md/CLAUDE.md/README | P2 | S |
| 042 | Close named coverage gaps | P2 | S-M |
| 043 | Hygiene sweep (parallel prepares, one-word parse, aliases, truthful viem/TS floors, CI pnpm cache) | P3 | S |

Ordering: 039 first, then 042/043 (shared files); 040 and 041 independent.

## Considered and rejected

`formatReason` `"undefined"` (likely unreachable) · `increaseAllowance` recognizer (conservative direction, nonstandard) · in-library text sanitization (docs-line mitigation instead) · state-override double-merge refactor (hot-path churn) · fixture-deploy dedup (inline reads better) · pre-commit hooks (CI is the gate) · parallel slot verification (collides with pinned RPC counts) · foundry-toolchain bump (Dependabot's job) · TS7 (too early) · `@types/node@20` vs Node 24 CI (correct posture) · vendored `typescript@5.6.1-rc` (attw's own) · Foundry nightly pin re-verified still required (stable v1.7.1 predates foundry#14569) · all prior-audit ledger rejections honored.
