---
"viem-tx-sim": patch
---

Internal ghost-contract refactor (no behavior or ABI change): restore tuple returns in the execution loop, consolidate probe helpers, uniform memory-safe assembly annotations, named selectors; corrected the via-IR rationale comment.
